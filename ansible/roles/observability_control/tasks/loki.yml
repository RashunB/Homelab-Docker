---
- name: Create Loki directories
  ansible.builtin.file:
    path: "{{ item }}"
    owner: ansible
    group: ansible
    mode: "0755"
    state: directory
  loop:
    - "{{ observability_control_root_dir }}/loki"
    - "{{ observability_control_root_dir }}/loki/data"

- name: Copy Loki files
  ansible.builtin.template:
    src: loki/loki-config.yml.j2
    dest: "{{ observability_control_root_dir }}/loki/loki-config.yml"
    owner: ansible
    group: ansible
    mode: "0644"
  notify: Restart Loki
