---
observability_control_cluster_name: "{{ cluster_name }}"
observability_control_root_dir: "{{ root_dir }}"
observability_control_control_host_ip: "{{ control_host_ip }}"
observability_control_pve_node_ip: "{{ pve_node_ip }}"

observability_control_prometheus_name: "{{ prometheus_name }}"
observability_control_prometheus_host_port: "{{ prometheus_host_port }}"

observability_control_node_exporter_name: "{{ node_exporter_name }}"
observability_control_node_exporter_host_port: "{{ node_exporter_host_port }}"

observability_control_cadvisor_name: "{{ cadvisor_name }}"
observability_control_cadvisor_host_port: "{{ cadvisor_host_port }}"

observability_control_smartctl_exporter_name: "{{ smartctl_exporter_name }}"
observability_control_smartctl_exporter_host_port: "{{ smartctl_exporter_host_port }}"
observability_control_smartctl_exporter_enabled: true

observability_control_grafana_name: "{{ grafana_name }}"
observability_control_grafana_host_port: "{{ grafana_host_port }}"

observability_control_alloy_name: "{{ alloy_name }}"
observability_control_alloy_host_port: "{{ alloy_host_port }}"
observability_control_alloy_con_port: "{{ alloy_con_port }}"
observability_control_alloy_journal_enabled: true

observability_control_loki_name: "{{ loki_name }}"
observability_control_loki_host_port: "{{ loki_host_port }}"
observability_control_loki_remote_url: "{{ loki_remote_url }}"

observability_control_dozzle_name: "{{ dozzle_name }}"
observability_control_dozzle_host_port: "{{ dozzle_host_port }}"

observability_control_dozzle_agent_pve_ip: "{{ dozzle_agent_pve_ip }}"

observability_control_pve_exporter_name: "{{ pve_exporter_name }}"
observability_control_pve_exporter_host_port: "{{ pve_exporter_host_port }}"
observability_control_pve_exporter_enabled: "{{ pve_exporter_enabled }}"
