---
- hosts: nas
  become: true
  tasks:
    - name: Configure LVM
      ansible.builtin.import_role:
        name: lvm_storage
      tags: lvm

    - name: Configure NFS
      ansible.builtin.import_role:
        name: nfs_server
      tags: nfs